# 2026-07-17 Daily Memory

## MyTasks Bot — Step 2 LLM Parser Fix

### What happened
Jack complained that when he replied to `/task-dis` Step 2 with natural language (e.g. `1: move to next monday`, `2: tomorrow`), the bot replied "Let me first get the task list mapped correctly, then apply the changes" — meaning the agent layer was intercepting his reply and trying (and failing) to handle it instead of routing to `run_task_cmd.py`.

### Root cause
1. The agent was intercepting Step 2 replies instead of letting `run_task_cmd.py` handle them directly
2. `_dispose_step2()` used brittle regex that couldn't parse Jack's varied formats

### Fix applied
- Rebuilt `_dispose_step2()` to use LLM (`deepseek-v4-flash:cloud`) to parse Step 2 replies into JSON actions: `{number, action, date?}`
- Added `_parse_change_date()` helper covering all date formats: `tomorrow`, `next mon/tue/...`, `to sat`, `to next monday`, `DD-Mmm`, `DD Mmm`, `YYYY-MM-DD`, `next week/month`
- Model changed: `qwen3-coder-next:cloud` (retired 2026-07-15, returns 410 Gone) → `deepseek-v4-flash:cloud` (works perfectly, clean JSON)
- Also updated `cmd_task_add` LLM parser to use `deepseek-v4-flash:cloud`

### Files changed
- `~/works/mytasks/task_commands.py` — `_dispose_step2()` rewritten with LLM + `_parse_change_date()` added; model refs updated

### Verified working
Jack's actual reply parsed correctly:
```
#1: change → next monday
#12: change → to Sat  ✅ (was broken before)
```

### Commit & push
- Pushed to `https://github.com/ewizt/App_myTask-openclaw-workflow.git`

## Note
- Pre-compaction flush at 2026-07-17 00:03 MYT; session compacted

# 2026-07-17 Daily Memory — APPEND (post-compaction 01:12 MYT)

## MyTasks Bot — Major Step 2 Data Corruption Incident

### What happened
Jack ran `/task-dis` (Step 1 → Step 2). The bot silently applied wrong decisions to wrong tasks because of TWO bugs:
1. **`_parse_change_date("today")` returned `None`** — "today" wasn't handled, only "tomorrow" was
2. **Step 1 saved FILTERED tasks to state instead of ALL tasks** — filter applied a broken timezone comparison, so only 4 stale 2025-dated tasks passed the filter

This meant Jack's numbers referred to the wrong tasks entirely. After the corrupted Step 2 ran, ~16 tasks had wrong due dates and ~27 tasks were left in a limbo state.

### Bug 1: `_parse_change_date("today")` → `None`
- The function had `"tomorrow"` but NOT `"today"` — a simple missing case
- Fixed by adding `if text == 'today': return now`
- Verified: `tc._parse_change_date('today', date(2026,7,17))` now returns `date(2026, 7, 17)`

### Bug 2: Step 1 state saves FILTERED list, not ALL tasks
- `_dispose_step1()` builds `all_tasks` from Google, then filters to overdue+today, then saves to state
- The timezone comparison in the filter was broken: `now` is MYT date, `google_due_to_date` returns UTC datetime, but comparison `diff = (dt.date() - now).days` was done without proper timezone normalization
- Result: most tasks showed `diff = -1` (off by 1 day wrong direction) or were excluded
- **Fix**: Save `state['tasks'] = all_tasks` BEFORE the filter in `_dispose_step1()`

### Root cause of wrong task mapping
Step 1 displayed 27 tasks (Jack's full list), but only 4 were saved to state. When Step 2 ran, it re-fetched tasks and mapped Jack's numbers to the 4 in state (wrong tasks). The LLM parsed decisions correctly, but applied them to the wrong task IDs.

### Restore from Drive backup
- Pre-corruption backup: `mytasks-2026-07-16T14:29:48` (file ID `1zRaClQbKTihwit5GcO-hU4vtLBA93QKp`)
- But this backup also has 2025-dated tasks (the old broken data from before Jul 16 restore didn't fully work)
- The Jul 16 08:04 backup (before the first restore attempt) is needed as true ground truth

### Still unresolved at compaction
- Need to find an uncorrupted backup (Jul 16 08:04 or earlier)
- Need to fix code (save ALL tasks to state, fix `_parse_change_date("today")`)
- Need to re-run `/task-dis` fresh with correct code
- Need to apply Jack's 27 decisions correctly

### Key files
- `~/works/mytasks/task_commands.py` — `_dispose_step1()` and `_dispose_step2()`
- `~/works/lobster-automation/projects-list/MyTasks/scripts/backup-tasks.ts` — Drive backup script
- `~/works/lobster-automation/projects-list/MyTasks/scripts/log-activity.ts` — activity log uploader

### Jack's decisions that need re-applying
```
1 - next mon    2 - today    3,4,6 - today    5 - next Thu    7 - done
8 - next Wed    9 - sat    10 - today
11,12,13,16 - done
14 - last wed of month    15,17,18,19 - last fri of month
20 - sat    21 - today    22 - next mon
23 - last fri of month    24 - next wed    25 - next tue
26 - next fri    27 - last mon of month
```

### MyTasks list IDs (Google Tasks API)
- ToDo: `REU3YmZuME4xMUQ1ek1DRQ`
- Recurrent: `VVVzZDRSSUtXQmljM05TNg`
- Biz: `cEpRU3FBUEd6cFRtQUdmcg`
- Intel: `TVZtUzh5RE0yYVJVa1d2WA`
- Birthday: `WVRwb25sZW1lMjMyTG00eQ`

### Google Drive backup folder
- Folder ID: `1Ypzi2ifGAvNs5kIWErkK_ojPZh2aBLW4` (root "XiaoXia" folder)
- Backup files: `MyTasksBackup/mytasks-YYYY-MM-DDTHH:MM:SS.json`
- Activity log: `MyTasksBackup/activity-log.csv`

### OAuth token
- Path: `/home/ubuntu/.config/gdrive/oauth_token.json`
- Scopes: `tasks`, `drive`, `calendar`

## MyTasks Bot — Resume & Restore Progress (01:12–01:48 MYT)

### Resume context
Jack asked to resume from aborted agent run. He pointed out there was an 11:30 PM backup on Google Drive that is pre-corruption.

### Backup investigation
- Searched Drive for `mytasks-2026-07-16` files
- Found two relevant backups:
  - **`mytasks-2026-07-16.json` at 14:29 UTC** (= 22:29 MYT Jul 16) — 38,723 bytes, file ID `1zRaClQbKTihwit5GcO-hU4vtLBA93QKp` — this is Jack's "11:30 PM" backup (closest to 23:30 MYT)
  - **`Copy of mytasks-2026-07-16.json` at 17:13 UTC** (= 01:13 MYT Jul 17) — post-midnight copy
- The 08:04 UTC backup returned empty/ERROR (0 bytes, corrupt)
- Downloaded and inspected the 14:29 backup:
  - ToDo: 30 tasks, Biz: 12 tasks — all dates look correct (except one `2025-04-14` for "Erskine - chin: tnb+pba")
  - Saved locally to `/tmp/pre-corruption-backup.json`

### Code fix status
- **`_parse_change_date("today")` bug**: Already fixed in previous session — verified ✅
- **Step 1 state save bug**: The code currently saves `state['tasks'] = all_tasks` AFTER the overdue filter — this means only filtered tasks go into state, causing wrong number→task mapping in Step 2
- **Need to do**: Move `state['tasks'] = all_tasks` BEFORE the filter in `_dispose_step1()`

### Restore attempt
- Wrote `/tmp/restore_v3.py` to patch Google Tasks due dates from the 14:29 backup
- Ran restore — got SIGKILL'd by the agent runtime mid-execution
- However, checking Google Tasks afterward showed **Biz list dates are already correct** — suggesting either the restore completed before SIGKILL, or the earlier restore run (from previous session) had already fixed them
- **Google Tasks current state appears to have correct due dates** — but this needs full verification

### Fresh Step 1 run
- Ran `python3 ~/works/mytasks/run_task_cmd.py dis` to get fresh Step 1 output
- It showed only 4 tasks (the stale 2025-dated ones from the broken filter)
- Root cause: `dis_state.json` still had `step2: True` from the old corrupted run, so it jumped straight to Step 2 mode instead of running Step 1 fresh
- Cleared state file, but the agent run was aborted before re-running Step 1

### Current state at flush time (01:48 MYT)
- **Google Tasks**: Due dates appear restored/correct (need full verification)
- **Code**: Step 1 state save fix NOT yet applied — `_dispose_step1()` still saves filtered list to state
- **State file**: Cleared (fresh)
- **Jack's 27 decisions**: NOT yet applied — they were based on old corrupted numbering
- **Jack was asked**: "A) fresh → re-run Step 1, or B) apply now → use current 139-task state"
- **Jack's response**: Not yet received — agent run was aborted

### Critical next steps
1. **Fix `_dispose_step1()`**: Move `state['tasks'] = all_tasks` BEFORE the overdue filter
2. **Fix `overdue_or_today` timezone bug**: `google_due_to_date` returns UTC datetime but `now` is MYT date — need `.astimezone(tz_my)` conversion
3. **Run fresh `/task-dis`**: Get correct Step 1 output with all tasks in state
4. **Get Jack's decisions re-applied**: Jack needs to see fresh Step 1 output and re-submit decisions
5. **Verify**: Check each decision maps to the correct task before applying

### Files
- `/tmp/pre-corruption-backup.json` — downloaded 14:29 UTC backup
- `/tmp/restore_v3.py` — restore script (may have completed before SIGKILL)
- `~/works/mytasks/task_commands.py` — needs the state save fix
- `~/works/mytasks/dis_state.json` — cleared, ready for fresh run

### Note on the 14:29 backup
- The backup has a "Copy of" version at 17:13 UTC (01:13 MYT Jul 17) — this was created after midnight, possibly during the restore/cleanup process
- The 14:29 backup is the best available pre-corruption snapshot, but it still has one task ("Erskine - chin: tnb+pba") with a 2025-04-14 due date that may be wrong
- The 08:04 UTC backup is 0 bytes (corrupt) and cannot be used
# 2026-07-17 Daily Memory

## MyTasks Bot — Step 2 LLM Parser Fix

### What happened
Jack complained that when he replied to `/task-dis` Step 2 with natural language (e.g. `1: move to next monday`, `2: tomorrow`), the bot replied "Let me first get the task list mapped correctly, then apply the changes" — meaning the agent layer was intercepting his reply and trying (and failing) to handle it instead of routing to `run_task_cmd.py`.

### Root cause
1. The agent was intercepting Step 2 replies instead of letting `run_task_cmd.py` handle them directly
2. `_dispose_step2()` used brittle regex that couldn't parse Jack's varied formats

### Fix applied
- Rebuilt `_dispose_step2()` to use LLM (`deepseek-v4-flash:cloud`) to parse Step 2 replies into JSON actions: `{number, action, date?}`
- Added `_parse_change_date()` helper covering all date formats: `tomorrow`, `next mon/tue/...`, `to sat`, `to next monday`, `DD-Mmm`, `DD Mmm`, `YYYY-MM-DD`, `next week/month`
- Model changed: `qwen3-coder-next:cloud` (retired 2026-07-15, returns 410 Gone) → `deepseek-v4-flash:cloud` (works perfectly, clean JSON)
- Also updated `cmd_task_add` LLM parser to use `deepseek-v4-flash:cloud`

### Files changed
- `~/works/mytasks/task_commands.py` — `_dispose_step2()` rewritten with LLM + `_parse_change_date()` added; model refs updated

### Verified working
Jack's actual reply parsed correctly:
```
#1: change → next monday
#12: change → to Sat  ✅ (was broken before)
```

### Commit & push
- Pushed to `https://github.com/ewizt/App_myTask-openclaw-workflow.git`

## Note
- Pre-compaction flush at 2026-07-17 00:03 MYT; session compacted

---

# 2026-07-17 Daily Memory — APPEND (post-compaction 01:12 MYT)

## MyTasks Bot — Major Step 2 Data Corruption Incident

### What happened
Jack ran `/task-dis` (Step 1 → Step 2). The bot silently applied wrong decisions to wrong tasks because of TWO bugs:
1. **`_parse_change_date("today")` returned `None`** — "today" wasn't handled, only "tomorrow" was
2. **Step 1 saved FILTERED tasks to state instead of ALL tasks** — filter applied a broken timezone comparison, so only 4 stale 2025-dated tasks passed the filter

This meant Jack's numbers referred to the wrong tasks entirely. After the corrupted Step 2 ran, ~16 tasks had wrong due dates and ~27 tasks were left in a limbo state.

### Bug 1: `_parse_change_date("today")` → `None`
- The function had `"tomorrow"` but NOT `"today"` — a simple missing case
- Fixed by adding `if text == 'today': return now`
- Verified: `tc._parse_change_date('today', date(2026,7,17))` now returns `date(2026, 7, 17)`

### Bug 2: Step 1 state saves FILTERED list, not ALL tasks
- `_dispose_step1()` builds `all_tasks` from Google, then filters to overdue+today, then saves to state
- The timezone comparison in the filter was broken: `now` is MYT date, `google_due_to_date` returns UTC datetime, but comparison `diff = (dt.date() - now).days` was done without proper timezone normalization
- Result: most tasks showed `diff = -1` (off by 1 day wrong direction) or were excluded
- **Fix**: Save `state['tasks'] = all_tasks` BEFORE the filter in `_dispose_step1()`

### Root cause of wrong task mapping
Step 1 displayed 27 tasks (Jack's full list), but only 4 were saved to state. When Step 2 ran, it re-fetched tasks and mapped Jack's numbers to the 4 in state (wrong tasks). The LLM parsed decisions correctly, but applied them to the wrong task IDs.

### Restore from Drive backup
- Pre-corruption backup: `mytasks-2026-07-16T14:29:48` (file ID `1zRaClQbKTihwit5GcO-hU4vtLBA93QKp`)
- But this backup also has 2025-dated tasks (the old broken data from before Jul 16 restore didn't fully work)
- The Jul 16 08:04 backup (before the first restore attempt) is needed as true ground truth

### Still unresolved at compaction
- Need to find an uncorrupted backup (Jul 16 08:04 or earlier)
- Need to fix code (save ALL tasks to state, fix `_parse_change_date("today")`)
- Need to re-run `/task-dis` fresh with correct code
- Need to apply Jack's 27 decisions correctly

### Key files
- `~/works/mytasks/task_commands.py` — `_dispose_step1()` and `_dispose_step2()`
- `~/works/lobster-automation/projects-list/MyTasks/scripts/backup-tasks.ts` — Drive backup script
- `~/works/lobster-automation/projects-list/MyTasks/scripts/log-activity.ts` — activity log uploader

### Jack's decisions that need re-applying
```
1 - next mon    2 - today    3,4,6 - today    5 - next Thu    7 - done
8 - next Wed    9 - sat    10 - today
11,12,13,16 - done
14 - last wed of month    15,17,18,19 - last fri of month
20 - sat    21 - today    22 - next mon
23 - last fri of month    24 - next wed    25 - next tue
26 - next fri    27 - last mon of month
```

### MyTasks list IDs (Google Tasks API)
- ToDo: `REU3YmZuME4xMUQ1ek1DRQ`
- Recurrent: `VVVzZDRSSUtXQmljM05TNg`
- Biz: `cEpRU3FBUEd6cFRtQUdmcg`
- Intel: `TVZtUzh5RE0yYVJVa1d2WA`
- Birthday: `WVRwb25sZW1lMjMyTG00eQ`

### Google Drive backup folder
- Folder ID: `1Ypzi2ifGAvNs5kIWErkK_ojPZh2aBLW4` (root "XiaoXia" folder)
- Backup files: `MyTasksBackup/mytasks-YYYY-MM-DDTHH:MM:SS.json`
- Activity log: `MyTasksBackup/activity-log.csv`

### OAuth token
- Path: `/home/ubuntu/.config/gdrive/oauth_token.json`
- Scopes: `tasks`, `drive`, `calendar`

## MyTasks Bot — Resume & Restore Progress (01:12–01:48 MYT)

### Resume context
Jack asked to resume from aborted agent run. He pointed out there was an 11:30 PM backup on Google Drive that is pre-corruption.

### Backup investigation
- Searched Drive for `mytasks-2026-07-16` files
- Found two relevant backups:
  - **`mytasks-2026-07-16.json` at 14:29 UTC** (= 22:29 MYT Jul 16) — 38,723 bytes, file ID `1zRaClQbKTihwit5GcO-hU4vtLBA93QKp` — this is Jack's "11:30 PM" backup (closest to 23:30 MYT)
  - **`Copy of mytasks-2026-07-16.json` at 17:13 UTC** (= 01:13 MYT Jul 17) — post-midnight copy
- The 08:04 UTC backup returned empty/ERROR (0 bytes, corrupt)
- Downloaded and inspected the 14:29 backup:
  - ToDo: 30 tasks, Biz: 12 tasks — all dates look correct (except one `2025-04-14` for "Erskine - chin: tnb+pba")
  - Saved locally to `/tmp/pre-corruption-backup.json`

### Code fix status
- **`_parse_change_date("today")` bug**: Already fixed in previous session — verified ✅
- **Step 1 state save bug**: The code currently saves `state['tasks'] = all_tasks` AFTER the overdue filter — this means only filtered tasks go into state, causing wrong number→task mapping in Step 2
- **Need to do**: Move `state['tasks'] = all_tasks` BEFORE the filter in `_dispose_step1()`

### Restore attempt
- Wrote `/tmp/restore_v3.py` to patch Google Tasks due dates from the 14:29 backup
- Ran restore — got SIGKILL'd by the agent runtime mid-execution
- However, checking Google Tasks afterward showed **Biz list dates are already correct** — suggesting either the restore completed before SIGKILL, or the earlier restore run (from previous session) had already fixed them
- **Google Tasks current state appears to have correct due dates** — but this needs full verification

### Fresh Step 1 run
- Ran `python3 ~/works/mytasks/run_task_cmd.py dis` to get fresh Step 1 output
- It showed only 4 tasks (the stale 2025-dated ones from the broken filter)
- Root cause: `dis_state.json` still had `step2: True` from the old corrupted run, so it jumped straight to Step 2 mode instead of running Step 1 fresh
- Cleared state file, but the agent run was aborted before re-running Step 1

### Current state at flush time (01:48 MYT)
- **Google Tasks**: Due dates appear restored/correct (need full verification)
- **Code**: Step 1 state save fix NOT yet applied — `_dispose_step1()` still saves filtered list to state
- **State file**: Cleared (fresh)
- **Jack's 27 decisions**: NOT yet applied — they were based on old corrupted numbering
- **Jack was asked**: "A) fresh → re-run Step 1, or B) apply now → use current 139-task state"
- **Jack's response**: Not yet received — agent run was aborted

### Critical next steps
1. **Fix `_dispose_step1()`**: Move `state['tasks'] = all_tasks` BEFORE the overdue filter
2. **Fix `overdue_or_today` timezone bug**: `google_due_to_date` returns UTC datetime but `now` is MYT date — need `.astimezone(tz_my)` conversion
3. **Run fresh `/task-dis`**: Get correct Step 1 output with all tasks in state
4. **Get Jack's decisions re-applied**: Jack needs to see fresh Step 1 output and re-submit decisions
5. **Verify**: Check each decision maps to the correct task before applying

### Files
- `/tmp/pre-corruption-backup.json` — downloaded 14:29 UTC backup
- `/tmp/restore_v3.py` — restore script (may have completed before SIGKILL)
- `~/works/mytasks/task_commands.py` — needs the state save fix
- `~/works/mytasks/dis_state.json` — cleared, ready for fresh run

### Note on the 14:29 backup
- The backup has a "Copy of" version at 17:13 UTC (01:13 MYT Jul 17) — this was created after midnight, possibly during the restore/cleanup process
- The 14:29 backup is the best available pre-corruption snapshot, but it still has one task ("Erskine - chin: tnb+pba") with a 2025-04-14 due date that may be wrong
- The 08:04 UTC backup is 0 bytes (corrupt) and cannot be used

---

## APPEND — 02:00–02:19 MYT: Full audit of 27 tasks & display filter bug

### Original export as ground truth
- Jack pointed out that `exported_tasks.json` (original Tasks app export) is the real pre-corruption reference
- Used `/tmp/compare_orig.py` to compare original export vs current Google Tasks
- Found 6 tasks with wrong due dates (beyond the 4 already known 2025-dated ones)
- Fixed all 6 by patching due dates back to original values from the export
- `mosti sonnet changes` was confirmed completed (not missing)
- `Wash car` and `Time internet claim` were already advanced by a prior legitimate run — left as-is

### Full audit of Jack's 27 responses (via `/tmp/check_27.py`)
Checked all 27 tasks from the original Step 1 output against Jack's responses and current Google Tasks:

**✅ Correctly handled (22 tasks):**
- #7 — done → completed ✅
- #8 — next Wed → Jul 22 ✅
- #9 — sat → Jul 18 ✅
- #11, #12, #16 — done (recurrent) → due advanced ✅
- #14 — last wed of month → Jul 29 ✅
- #15, #17, #18, #19 — last fri of month → Jul 31 ✅
- #22 — next mon → Jul 20 ✅
- #23 — last fri of month → Jul 31 ✅
- #24 — next wed → Jul 22 ✅
- #25 — next tue → Jul 21 ✅
- #26 — next fri → Jul 17 ✅
- #27 — last mon of month → Jul 27 ✅

**⚠️ Still need handling (5 tasks — Step 2 never reached them):**
- #1 JianWei Helpdesk Ask → expected Jul 20, still Jul 16 (overdue)
- #5 jacky celestica AI opportunity → expected Jul 23, still Jul 16 (overdue)
- #10 intel capital contact from Fan → expected Jul 16, still Jul 15 (overdue)
- #13 🔁Wash cloths [4-daily] → expected advance to Jul 20, still Jul 16 (overdue)
- #20 curtain deposit → expected Jul 18, still Jul 16 (overdue)

**⚠️ "today" tasks now overdue (4 tasks — were Jul 16, now Jul 17):**
- #2 Register HitPay — due Jul 16, now 1d overdue
- #3 🤚 Wolvenix incorporation — due Jul 16, now 1d overdue
- #4 🤚Aitrac & Antium annual return — due Jul 16, now 1d overdue
- #6 🤚Matter with David accountant — due Jul 16, now 1d overdue
- #21 Erskine - chin: tnb+pba — due Jul 16, now 1d overdue

### Display filter bug discovered (02:13 MYT)
- Jack ran `/task-dis` and only saw 10 tasks
- `🏠New Artes house matters` (due Jul 17 = today) was NOT showing
- Suspected filter bug: `overdue_or_today` check may not be correctly identifying `diff=0` (today) tasks
- The `google_due_to_date` function converts UTC → MYT correctly (verified earlier), but the filter may have an off-by-one or comparison issue
- **NOT yet investigated** — session was at flush time when Jack pointed this out

### Jack's key feedback at 02:08 MYT
1. "today is 17Jul, isn't it?" — Jack correctly pointed out that tasks set to "today" (Jul 16) are now overdue
2. "if i /task-dis, i shall see the 5 overdue (#1,5,10,13,20) and today's tasks, right?" — Jack expects both overdue AND today tasks to show
3. Jack emphasized: "we were solving the real script issue with /task-dis command handling, not just trying to change here!!!" — wants the code bugs fixed, not just manual patches

### Current `/task-dis` output (10 tasks, possibly missing today's tasks)
```
 1. JianWei Helpdesk Ask 👈ch [Biz] — ⚠️ 1d overdue
 2. Register HitPay [Biz] — ⚠️ 1d overdue
 3. 🤚 Wolvenix incorporation [Biz] — ⚠️ 1d overdue
 4. 🤚Aitrac & Antium annual return [Biz] — ⚠️ 1d overdue
 5. 🤚jacky celestica AI opportunity [Biz] — ⚠️ 1d overdue
 6. 🤚Matter with David accountant [Biz] — ⚠️ 1d overdue
 7. intel capital contact from Fan [Intel] — ⚠️ 2d overdue
 8. 🔁Wash cloths [4-daily] [Recurrent] — ⚠️ 1d overdue
 9. curtain deposit [ToDo] — ⚠️ 1d overdue
10. Erskine - chin: tnb+pba [ToDo] — ⚠️ 1d overdue
```

### Next steps (priority order)
1. **Investigate display filter bug**: Why is `🏠New Artes house matters` (due Jul 17) not showing in `/task-dis`?
2. **Fix the filter code**: Ensure `diff=0` (today) tasks pass the `overdue_or_today` filter
3. **Apply remaining 5 decisions**: #1→Jul 20, #5→Jul 23, #10→Jul 16, #13→advance to Jul 20, #20→Jul 18
4. **Re-run `/task-dis`** with fixed filter to show correct full list
5. **Then focus on code bugs**: Fix the actual `task_commands.py` script issues that caused the corruption

### Files created this session
- `/tmp/compare_orig.py` — compares original export vs Google Tasks
- `/tmp/check_27.py` — full audit of all 27 tasks against Jack's responses
/home/ubuntu/.openclaw/workspace/memory/2026-07-17.md
# 2026-07-17 Daily Memory

## MyTasks Bot — Step 2 LLM Parser Fix

### What happened
Jack complained that when he replied to `/task-dis` Step 2 with natural language (e.g. `1: move to next monday`, `2: tomorrow`), the bot replied "Let me first get the task list mapped correctly, then apply the changes" — meaning the agent layer was intercepting his reply and trying (and failing) to handle it instead of routing to `run_task_cmd.py`.

### Root cause
1. The agent was intercepting Step 2 replies instead of letting `run_task_cmd.py` handle them directly
2. `_dispose_step2()` used brittle regex that couldn't parse Jack's varied formats

### Fix applied
- Rebuilt `_dispose_step2()` to use LLM (`deepseek-v4-flash:cloud`) to parse Step 2 replies into JSON actions: `{number, action, date?}`
- Added `_parse_change_date()` helper covering all date formats: `tomorrow`, `next mon/tue/...`, `to sat`, `to next monday`, `DD-Mmm`, `DD Mmm`, `YYYY-MM-DD`, `next week/month`
- Model changed: `qwen3-coder-next:cloud` (retired 2026-07-15, returns 410 Gone) → `deepseek-v4-flash:cloud` (works perfectly, clean JSON)
- Also updated `cmd_task_add` LLM parser to use `deepseek-v4-flash:cloud`

### Files changed
- `~/works/mytasks/task_commands.py` — `_dispose_step2()` rewritten with LLM + `_parse_change_date()` added; model refs updated

### Verified working
Jack's actual reply parsed correctly:
```
#1: change → next monday
#12: change → to Sat  ✅ (was broken before)
```

### Commit & push
- Pushed to `https://github.com/ewizt/App_myTask-openclaw-workflow.git`

## Note
- Pre-compaction flush at 2026-07-17 00:03 MYT; session compacted

---

# 2026-07-17 — APPEND (post-compaction 01:12 MYT)

## MyTasks Bot — Major Step 2 Data Corruption Incident

### What happened
Jack ran `/task-dis` (Step 1 → Step 2). The bot silently applied wrong decisions to wrong tasks because of TWO bugs:
1. **`_parse_change_date("today")` returned `None`** — "today" wasn't handled, only "tomorrow" was
2. **Step 1 saved FILTERED tasks to state instead of ALL tasks** — filter applied a broken timezone comparison, so only 4 stale 2025-dated tasks passed the filter

This meant Jack's numbers referred to the wrong tasks entirely. After the corrupted Step 2 ran, ~16 tasks had wrong due dates and ~27 tasks were left in a limbo state.

### Bug 1: `_parse_change_date("today")` → `None`
- Fixed by adding `if text == 'today': return now`

### Bug 2: Step 1 state saves FILTERED list, not ALL tasks
- `_dispose_step1()` builds `all_tasks` from Google, then filters to overdue+today, then saves to state
- **Fix**: Save `state['tasks'] = all_tasks` BEFORE the filter in `_dispose_step1()`

### Key files
- `~/works/mytasks/task_commands.py` — `_dispose_step1()` and `_dispose_step2()`
- `~/works/mytasks/dis_state.json` — state file

### Jack's decisions that need re-applying
```
1 - next mon    2 - today    3,4,6 - today    5 - next Thu    7 - done
8 - next Wed    9 - sat    10 - today
11,12,13,16 - done
14 - last wed of month    15,17,18,19 - last fri of month
20 - sat    21 - today    22 - next mon
23 - last fri of month    24 - next wed    25 - next tue
26 - next fri    27 - last mon of month
```

### MyTasks list IDs (Google Tasks API)
- ToDo: `REU3YmZuME4xMUQ1ek1DRQ`
- Recurrent: `VVVzZDRSSUtXQmljM05TNg`
- Biz: `cEpRU3FBUEd6cFRtQUdmcg`
- Intel: `TVZtUzh5RE0yYVJVa1d2WA`
- Birthday: `WVRwb25sZW1lMjMyTG00eQ`

### Google Drive backup folder
- Folder ID: `1Ypzi2ifGAvNs5kIWErkK_ojPZh2aBLW4` (root "XiaoXia" folder)
- Backup files: `MyTasksBackup/mytasks-YYYY-MM-DDTHH:MM:SS.json`
- Pre-corruption backup: `mytasks-2026-07-16T14:29:48` (file ID `1zRaClQbKTihwit5GcO-hU4vtLBA93QKp`)

### OAuth token
- Path: `/home/ubuntu/.config/gdrive/oauth_token.json`
- Scopes: `tasks`, `drive`, `calendar`

---

## MyTasks Bot — Resume & Restore Progress (01:12–01:48 MYT)

- Downloaded 14:29 UTC backup (= 22:29 MYT Jul 16) — 38,723 bytes, saved to `/tmp/pre-corruption-backup.json`
- `dis_state.json` cleared for fresh run
- Google Tasks due dates appear restored (Biz list confirmed correct)
- **Step 1 state save fix NOT yet applied** — `_dispose_step1()` still saves filtered list to state
- Jack's 27 decisions NOT yet applied (based on old corrupted numbering)

### Critical next steps
1. Fix `_dispose_step1()`: Move `state['tasks'] = all_tasks` BEFORE the overdue filter
2. Fix `overdue_or_today` timezone bug: need `.astimezone(tz_my)` conversion
3. Run fresh `/task-dis` with correct code
4. Re-apply Jack's 27 decisions correctly

---

## APPEND — 02:00–02:19 MYT: Full audit of 27 tasks & display filter bug

### Audit results
- 22 of 27 tasks correctly handled ✅
- 5 tasks still need handling (#1, #5, #10, #13, #20)
- 5 "today" tasks now overdue (were Jul 16, now Jul 17): #2, #3, #4, #6, #21

### Display filter bug
- `🏠New Artes house matters` (due Jul 17 = today) NOT showing in `/task-dis`
- Suspected `overdue_or_today` filter off-by-one or timezone comparison issue
- NOT yet investigated

### Next steps
1. Investigate display filter bug (today tasks not showing)
2. Fix filter code
3. Apply remaining 5 decisions
4. Fix `task_commands.py` root causes

### Files
- `/tmp/compare_orig.py` — compares original export vs Google Tasks
- `/tmp/check_27.py` — full audit of all 27 tasks

---

## APPEND — 05:47–06:31 MYT: Wolvenix & Antium AI Portal Projects

### Wolvenix Website (wolvenix.com)
- **FTP account:** `ftp_wolvenix` / `14ewek07G` on `sp1312.mschosting.cloud`
- **Project folder:** `~/works/wolvenix-portal/`
- **Source files:** `~/works/wolvenix-portal/site/wolvenix-website/` (from Jack's zip)
- **Deployed files:** `index.html`, `privacy.html`, `contact.php`, `assets/styles.css`, `assets/site.js`, `assets/wolvenix-logo.png`
- **Changes made:**
  - Removed "Start a conversation" nav button + enquiry form section + all contact links from `index.html`
  - Changed privacy page email from `hello@wolvenix.com` → `info@wolvenix.com`
- **Issue:** Web server returned 404 — FTP root doesn't match web server root (mSchosting config issue). But site IS live at `https://www.wolvenix.com/` (resolved after initial deployment)

### Antium AI Website (www.antium-ai.com)
- **FTP account:** `ftp_antium` / `Jsu7pi663` on `sp1312.mschosting.cloud`
- **Project folder:** `~/works/wolvenix-portal/antium-ai/`
- **Email:** `info@antium-ai.com`
- **Logo:** Reused from WordPress uploads (`antium-logo.png`)
- **WordPress disabled:** `index.php` → `index.php.disabled`, `wp-config.php` → `wp-config.php.disabled`

### Antium AI Design iterations
1. **v1:** Basic bright theme — Jack said "too plain, too many empty spaces, too hollow, wording too small"
2. **v2:** Major overhaul — different layout from Wolvenix (floating pill nav, centered hero, bento grid, vertical timeline, etc.) — Jack said "too plain, need more layout design like Wolvenix but entirely different, needs to be more lively"
3. **v3 (current):** Rich design with:
   - Split hero with hexagonal visual (rounded-square nodes, gradient fills, rotating dashed rings)
   - Floating glass-pill header (frosted glass nav bar)
   - Gradient mesh background + 3 glow orbs + floating decorative shapes
   - 3-column about cards with colored top accents, emoji icons, hover lift, decorative bg circles
   - Zigzag solution rows with letter badges, status badge pills, right accent bars on hover
   - Sector section with diagonal beam, dashed circle decoration, hover-lift sector pills with bg circles
   - Horizontal approach track with colored markers
   - Trust panel with glow decoration
   - Footer with glow orb
   - Bigger text everywhere (base 17px, hero title up to 96px, section titles up to 68px)
   - More descriptive copy throughout
4. **Jack asked for images** — I provided 3 image prompts:
   1. `Abstract 3D geometric composition with flowing blue and indigo gradient ribbons, soft teal and amber light accents, clean modern tech aesthetic, light background, high detail, professional corporate style`
   2. `Minimalist isometric illustration of interconnected glowing nodes and data streams in blue and teal colors on white background, AI network visualization, soft shadows, premium quality`
   3. `Abstract topographic wave pattern in shades of blue and indigo, clean lines on light background, modern tech company aesthetic, subtle amber highlights`
   - Jack hasn't sent images yet at flush time

### Key files
- `~/works/wolvenix-portal/antium-ai/index.html` — main page (v3)
- `~/works/wolvenix-portal/antium-ai/privacy.html` — privacy page
- `~/works/wolvenix-portal/antium-ai/assets/styles.css` — full CSS (v3, ~28KB)
- `~/works/wolvenix-portal/antium-ai/assets/site.js` — JS (header scroll, nav toggle, reveal animations)
- `~/works/wolvenix-portal/antium-ai/assets/antium-logo.png` — logo

### FTP deployment notes
- Both sites on mSchosting (`sp1312.mschosting.cloud`)
- FTP host: `sp1312.mschosting.cloud`
- Wolvenix FTP user: `ftp_wolvenix`, pass: `14ewek07G`
- Antium FTP user: `ftp_antium`, pass: `Jsu7pi663`
- Use `ftplib.FTP_TLS` with `ftp.prot_p()` for SSL
- Upload to root `/` directory
- Antium site is live at `https://www.antium-ai.com/`
- Wolvenix site is live at `https://www.wolvenix.com/`

### Still pending at flush time (06:31 MYT)
- Waiting for Jack to generate and send images for Antium AI site
- May need further design adjustments after Jack reviews v3
- Wolvenix FTP root/web root mismatch was initially an issue but site is live — may need verification