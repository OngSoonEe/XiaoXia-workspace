# Memory — 2026-07-03

## Cloudflare Zone — LIVE and Working

Despite Cloudflare dashboard showing `pending`, **DNS is fully operational**:

- `ewizt.com` NS → `dante.ns.cloudflare.com` + `gail.ns.cloudflare.com` ✅ (NS switch propagated globally)
- `automation.ewizt.com` CNAME → `eef22362-998e-4c28-bc74-9eee54aa32c2.cfargotunnel.com` ✅
- Tunnel `automation-v3` (ID: `eef22362-998e-4c28-bc74-9eee54aa32c2`) — **healthy**, 4 active edge connections
- Lobster server at `:3099` — responding `{"ok":true}` ✅
- Jack tested from phone on LTE — confirmed working: `https://automation.ewizt.com/health` returns `{"ok":true}`

**Zone status discrepancy:** Cloudflare API shows `status: "pending"` even though public DNS is routing correctly through Cloudflare NS. This is a Cloudflare backend UI inconsistency — functionally the zone is active. The CNAME record ID in Cloudflare DNS: `c3e208a2b3ca55932b33f71aa2048b67`.

## Actions Taken Today

- Restarted cloudflared tunnel (idle overnight stale connections)
- Updated CLOUDFLARE-TUNNEL.md with current architecture, credentials, DNS setup, troubleshooting
- Created `/home/ubuntu/restart-tunnel.sh` — quick tunnel restart script
- Set up 2-hourly cron check for zone status (originally 30 min, changed by Jack)

## Cron Job

- Zone check every 2 hours: checks Cloudflare API for zone status, will announce in Telegram group when `status: "active"` (though functionally already working)

## Key Lesson

Cloudflare dashboard/zone status can show `pending` even when DNS is already routing correctly globally. Public DNS checks (external resolvers) are more reliable than the Cloudflare API status field for determining real-time availability.


## Lobster Automation — Projects Directory Change

- **Projects moved:** `projects-list/` is now the sole active projects directory. `~/projects/` is ignored.
- **PROJECTS_DIR** env var: `/home/ubuntu/works/lobster-automation/projects-list`
- Symlink-following code in `server/src/server.ts` was reverted (no longer needed)

## email-nudge Project — Complete

`projects-list/email-nudge/`:
- `taskflows/email-nudge.yaml` — webhook trigger named `nudge`
- `scripts/send.ts` — reads `LOBSTER_PREVIOUS_RESULTS`, calls `/api/generate-subject` for AI subject, sends HTML email
- `.env` — `SMTP_PASSWORD`, `LOBSTER_SERVER_URL=http://localhost:3099`
- `package.json` — deps: nodemailer, zod, tsx, dotenv

## AI Subject Generation — Pattern

The lobster server (this environment) has Ollama access. Pattern:
1. Lobster server exposes `POST /api/generate-subject` — calls local Ollama
2. Script step calls `http://localhost:3099/api/generate-subject` to get subject
3. Falls back to title-casing first sentence if server unreachable

**Ollama is reachable here:** `http://localhost:11434` (Windows Ollama on host, bridged to WSL)
**Ollama cloud:** `https://ollama.com/api` — blocked from this Linux env (needs auth)

## Browser Form — email-nudge

- `GET /form/nudge` — HTML form (recipient + message fields, URL-encoded POST)
- `POST /form/nudge` — accepts URL-encoded, triggers flow same as webhook
- Form handler added to `server/src/router.ts` (no external deps, just Fastify)

## Key Server Fixes This Session

1. `server/src/server.ts` — fixed `autoDiscoverProjects`: used `e.name` (entry name) not full path for `taskflowsDir` check
2. `step-runner.ts` — script steps now receive `LOBSTER_PREVIOUS_RESULTS` env var with full context
3. `@fastify/formbody` installed in `server-deployment/` for URL-encoded form support

## Commits Pushed (2026-07-03)

- `06d6c48` — v0.2: Smart Retry + Selective Replay + Watchdog
- `4c4f47e` — email-nudge project + server fixes + form page
- Plus 3 subsequent commits for form body plugin, router update, send.ts rewrite

