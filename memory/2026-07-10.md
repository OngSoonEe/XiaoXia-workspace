# 2026-07-10 Daily Notes

## Home Directory Cleanup Session (00:36–01:02 AM)

Jack asked to review and clean up `~/` directory. Significant cleanup done:

### Deleted
- `bun.lock` — leftover bun lockfile
- `openclaw-gateway.log` — old OpenClaw gateway log (May 5)
- `go/`, `goinst/`, `gopath/` — ~2 GB of unused Go installations (no Go projects exist, Go not used)
- `intelephense/` — empty PHP IntelliSense cache
- `data/` — old simplecms SQLite DB (979 KB, May 4)
- `~/topics/guangzhou26/` — local-only git repo, no remote
- `~/projects/` — all old pre-Lobster projects (daily-news-tech, daily-news-tech-site, daily-news-esg, daily-news-general, daily-news-general-site, SiNova, .trash-context-clean). Committed + pushed to `github.com/Wolvenix-ai/Data_OpenclawWorkflow.git` before deletion. 268 MB freed.
- `~/code/` — old SW Factory / feedback system project (2.2 MB). Git repo pointing to `github.com/DataInsightAutomation/UseCase-FeedbackSystemPBB.git`. Last touched May 7.
- `~/archived/` — empty folder
- `~/projects/email-nudge` symlink — pointed to Lobster's email-nudge project, no longer needed

### Kept
- `~/bin/` — binaries (cloudflared etc)
- `~/topics/` — Jack's topic repos (local git, no remote except root which has none)
- `~/works/` — Lobster Automation + novel_fuzzy-weights (active)
- `~/works/knowledgeGraph/` — has requirements.txt, Jack said keep
- `restart-openclaw.sh` — OpenClaw restart script (not mine)

### Also cleaned
- `.bashrc` Go entries removed (GOROOT, GOPATH etc)

## wsl-house-clean Workflow Updated (00:58–01:02 AM)

After the directory cleanup, updated `wsl-house-clean` scripts to reflect the changed directory structure:

### Scripts Fixed (commit `0713f04`)
- `scan-workspaces.ts` — scans `~/works/` instead of deleted `~/code/`
- `scan-storage.ts` — scans `~/works/` instead of deleted `~/projects/` + `~/code/`
- `git-push-projects.ts` — removed `~/projects/` umbrella repo logic entirely; now only pushes individual repos under `~/works/`
- `notify-telegram.ts` — uses `process.cwd()` instead of hardcoded `~/projects/context-clean` path
- 65 lines dead code removed, 12 lines added

### Remaining ~/ Structure
```
~/bin/
~/topics/
~/works/ (lobster-automation/, novel_fuzzy-weights/, knowledgeGraph/)
~/restart-openclaw.sh
```
Much cleaner than before — from ~2.5 GB dead weight down to just active stuff.

## Context
- This was all done in Telegram group "XiaoXia" → "Lobster Automation" topic
- Jack was reviewing and cleaning up his home directory after the ewizt.com block / server stabilization work from Jul 7-8
- All Lobster Automation work is in `~/works/lobster-automation/` (active, Git-tracked)