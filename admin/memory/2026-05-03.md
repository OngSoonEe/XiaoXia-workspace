# Memory 2026-05-03

## SW Factory v3 Tag Moved
- Moved `v3-sandbox` tag to latest commit `c45c523` (was `270edbf`)

## Cleanup Fix Applied
- Rewrote `cleanup_factory_state()` in `swfactory_scaffold.py` — now conservative by default
- Old logic: removed bindings with no session files (too aggressive — sessions are ephemeral)
- New logic: scans `~/code/` for scaffolded projects (SKILL.md presence) as context, only removes when confirmed stale
- When in doubt, keeps bindings

## Improvement Plan Analysis (Jack's 7 Questions)

Jack asked for honest analysis on scaling improvements. Key decisions:

1. **DAG in WORK_ORDERS.md** — Yes, sw_planner adds `depends_on` field, conditional on WO count > 5. ~30 min effort.
2. **Parallel dev agents** — Skip. 3-job Ollama limit + merge conflict problem makes it impractical.
3. **Persistent sessions** — Wrong framing. Real solution: add `MEMORY.md` + `PROGRESS.md` per project workspace. Agents read on spawn, write learnings/summaries. This is the CLAUDE.md / Claude Code auto-memory pattern — proven, no infra needed. ~1 hr effort.
4. **CI/CD** — Defer. Not needed yet. Verifier already runs sandbox tests. CI/CD valuable when frequent commits by multiple people. Can add webhook → orchestrator → spawn verifier later in ~20 lines.
5. **Human approval gates** — Already implicit (orchestrator controls transitions). Add one line to SOUL.md: "never transition without explicit user approval." No mechanism needed.
6. **UAT depth** — Rewrite UAT SOUL to produce **Playwright test specs** (`uat/specs/*.spec.ts`) instead of manual clicking. Run with `npx playwright test`. Auto-capture screenshots on failure. Turns UAT from vibes into repeatable tests. Playwright already installed. ~1 hr effort.
7. **Context window management** — Same as #3. MEMORY.md + PROGRESS.md solves it. No vector DB, no context compression, no custom cache needed. Anthropic's own recommendation is simple composable files.

### Priority Actions (if Jack approves)
- Add MEMORY.md + PROGRESS.md to scaffold template (~1 hr)
- Add `depends_on` field to planner SOUL (~30 min)
- Rewrite UAT SOUL for Playwright test specs (~1 hr)
- Add "never transition without explicit user approval" to SOUL.md (~5 min)
## v4-context-recovery — All Three Improvements Implemented

### #1 DAG Work Orders
- Threshold changed from 5 → 7 (simple flat format for ≤7 WOs, DAG with `depends_on` for ≥8)
- Rationale: most 6-7 WO projects are sequential; only complex multi-feature projects need DAG
- App_nini2 has 9 WOs but was created before DAG existed — flat list, not auto-upgraded
- Planner adds `depends_on` field; dev checks prerequisites before starting a WO
- WORK_ORDERS.md template in scaffold includes HTML comment format guide

### #3 Context Recovery (MEMORY.md + PROGRESS.md)
- All 9 agents now have MANDATORY 6-step context recovery preamble:
  1. Read MEMORY.md
  2. Read PROGRESS.md
  3. If can't read, STOP and ask
  4. Before finishing, APPEND to PROGRESS.md
  5. Write learnings to MEMORY.md
  6. Confirm "PROGRESS.md updated"
- Scaffold creates PROGRESS.md template on new projects, preserves on `--update`
- App_nini2 PROGRESS.md created with existing debugger entry as seed
- App_nini2's 7 SOUL files manually updated with MANDATORY context recovery (since `--update` preserves existing SOULs)

### #6 UAT Playwright Test Specs
- UAT now does BOTH exploratory browser testing AND automated Playwright test specs
- Creates `/uat/specs/*.spec.js` mapping to acceptance criteria
- Includes Playwright spec template and install instructions
- Non-UI projects get `/uat/scripts/*.sh` instead
- Debugger updated to re-run Playwright specs after fixes
- UAT_REPORT.md references specific spec files for each criterion

### Integrity Verification
- 42/42 checks passed: all 9 catalog agents + 7 App_nini2 SOULs have MANDATORY context recovery
- All model references correct (no legacy models)
- No ghost agent entries in openclaw.json
- UAT has 9 Playwright references, debugger has 1 (re-run), architect/planner have 1 (search mandate)

### Git State
- Commit: `1db7886` — README updated with v4 sections
- Tag: `v4-context-recovery` → `1db7886`
- All tags: v1-basic-working, v2-uat-enhanced, v3-sandbox, v4-context-recovery

### Remaining TODO (not yet done)
- Add "never transition without explicit user approval" line to orchestrator SOUL.md (~5 min)
- End-to-end test of App_nini2 UAT phase
- Verify UAT-debugger feedback loop works end-to-end